# AGENTS.md

## Context

- Project: LESS (LLM Empowered Semantic Search)
- Type: Python CLI
- Commands: `index`, `search`
- Behavior: show step-by-step progress, report errors, exit cleanly

## Testing

- Default to `pytest`; run with `python -m pytest`.
- Place tests in `tests/` and name files `test_*.py`.
